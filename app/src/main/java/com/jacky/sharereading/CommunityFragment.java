package com.jacky.sharereading;

import android.app.Fragment;
import android.os.Bundle;
import android.support.annotation.Nullable;
import android.view.View;

/**
 * Created by jackydu on 2017/10/11.
 * the community module
 * users can publish some share message in this module which has been approved by manager
 * and the others can get these bulletins message and connect with the publisher;
 * 于此同时，如果你对有些书或者阅读活动感兴趣的话，你可以和发布者联系，进行线下置换，或者加入某些同城的读书活动
 */

public class CommunityFragment extends Fragment {

    @Override
    public void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
    }

    @Override
    public void onViewCreated(View view, @Nullable Bundle savedInstanceState) {
        super.onViewCreated(view, savedInstanceState);
    }
}
